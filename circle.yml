deployment:
  staging:
    branch: master
    heroku:
      appname: hotel-booking-resize-service

machine:
  java:
    version: oraclejdk8
  environment:
    DATABASE_URL: jdbc:mysql://localhost:3306/circle_test?user=ubuntu&password=

dependencies:
  override:
    - chmod +x gradlew
    - ./gradlew dependencies
    